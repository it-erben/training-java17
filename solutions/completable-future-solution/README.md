# CompletableFuture – Lösung

Referenzimplementierung zum Modul `assignments/completable-future`. Alle Übungen in
`tech.erben.java17.completablefuture.CompletableFutureWorkshop` sind vollständig ausgefüllt, inklusive Kombinieren, Komponieren, Fallbacks und Timeouts.

Tests ausführen:
```bash
mvn -pl solutions/completable-future-solution test -DskipTests=false
```
